# 校园AI助手全流程开发总结
以下内容基于所有流程图收敛而成，**完全贴合你的开发技术栈（Debian13 + Python/uv + MySQL/Milvus + Vue3）**，明确了每个模块的核心闭环、开发优先级、必做节点和上下游依赖，可直接作为开发 roadmap 使用。

---

## 一、系统整体架构总览（全流程顶层逻辑）
所有功能模块均遵循「**4层架构**」开发，严格分层解耦，核心能力可复用，避免重复开发：
| 架构分层 | 核心职责 | 对应技术栈 | 开发优先级 |
|----------|----------|------------|------------|
| 基础底座层 | 环境部署、用户认证、权限控制、数据库操作封装、通用工具类 | Debian13、Python/uv、MySQL、JWT | 最高（必须最先开发） |
| AI能力层 | LLM调用封装、向量嵌入、文档处理、图像识别、NLP预处理 | Milvus、Embedding模型、mineru、LLM API/本地模型 | 次高（所有业务模块的核心依赖） |
| 业务服务层 | 各功能模块的业务逻辑、接口开发、流程闭环实现 | Python后端框架（推荐FastAPI）、APScheduler定时任务 | 中高（按优先级分模块开发） |
| 前端交互层 | 页面渲染、用户操作、接口对接、交互体验优化 | Vue3 + HTML/CSS | 与对应后端业务模块并行开发 |

---

## 二、分模块核心流程开发总结（按开发优先级排序）
### 模块1：系统基础与用户认证模块（所有功能的前置依赖）
#### 核心开发目标
实现用户身份校验、角色权限隔离、全局路由分发，是所有业务模块的入口，必须**第一个开发完成**。
#### 流程闭环总结
`用户打开应用 → 登录/注册 → 学号/工号身份校验 → 角色分配（学生/教师/管理员）→ 主界面路由 → 业务模块分发`，全链路闭环，所有业务模块必须依赖用户身份与权限信息。
#### 必做开发节点
1.  MySQL用户核心表设计（含学号、角色、个人标签、密码哈希、隐私设置等字段）
2.  认证接口开发：登录、注册、Token签发/刷新、身份校验、未登录拦截
3.  三级权限控制：学生/教师/管理员的接口权限、数据权限隔离
4.  全局路由与主界面框架开发，完成模块入口分发
#### 上下游关联
所有业务模块的前置依赖，无此模块，其他功能无法开发。

---

### 模块2：RAG校园知识检索模块（AI能力核心底座）
#### 核心开发目标
实现校园数据的自然语言问答，同时沉淀**向量检索、LLM调用、文档处理**三大通用能力，给赛事、地图、论坛模块复用，**第二个开发**。
#### 流程闭环总结
分为两条独立闭环，先开发离线数据链路，再开发在线问答链路：
1.  **离线数据处理闭环**：校园数据采集 → 清洗去重/分类 → 向量嵌入 → Milvus向量库+MySQL元数据双库存储
2.  **在线问答闭环**：用户自然语言提问 → NLP预处理/意图识别 → 向量检索Top-K相似内容 → 拼接上下文 → LLM生成答案 → 返回用户
#### 必做开发节点
1.  通用工具封装：LLM调用、Embedding生成、MySQL/Milvus数据库操作、文档清洗工具
2.  离线数据链路开发：校园数据采集脚本、数据分类规则、向量入库逻辑
3.  在线问答接口开发：提问、检索、答案生成全流程接口
4.  前端问答页面开发
#### 上下游关联
为赛事、地图、论坛模块提供核心AI能力复用，是所有AI相关功能的底座。

---

### 模块3：赛事智能推荐与组队模块（你的核心需求，重点开发）
#### 核心开发目标
实现「**AI全流程自动化处理，学生零杂活极简操作**」，完成赛事数据从爬取到学生组队参赛的全链路闭环，**第三个开发**，可直接复用RAG模块的AI能力。
#### 全流程闭环总结
严格按以下4个串行子流程开发，前一个流程跑通再进入下一个：
1.  **数据爬取与文档预处理闭环**：定时/增量定向爬取 → 原始数据兜底存入MySQL → PDF附件调用mineru转Markdown → 多格式归一化 → 文本清洗 → 输出标准化纯文本
2.  **AI智能处理闭环**：标准化文本输入 → AI提取学生决策最小核心信息 → 生成极简摘要 → 按校赛/省赛/国赛分档 → 赛道分类/标签化 → 合规校验/异常数据标记
3.  **自动化分档入库闭环**：校验通过数据 → MySQL结构化分表存储 → 赛事文本生成向量嵌入 → Milvus向量库同步入库 → 双库数据关联 → 触发推荐列表更新
4.  **学生端极简操作闭环**：AI基于用户标签个性化推荐 → 学生浏览极简核心信息 → 一键报名 → AI匹配技能互补队友 → 组队成功 → AI拆分任务/全周期节点提醒
#### 必做开发节点（按开发顺序）
1.  爬虫模块：定向爬取源配置、定时/增量爬取、去重逻辑、原始数据兜底存储
2.  文档预处理模块：mineru工具集成、多格式转Markdown、文本归一化清洗
3.  AI处理模块：核心信息提取Prompt开发、分档打标逻辑、合规校验、异常处理
4.  双库入库模块：MySQL赛事相关表设计、Milvus向量库配置、双库关联逻辑
5.  匹配算法：个性化赛事推荐、队友智能匹配逻辑
6.  业务接口：赛事列表、组队申请、协同任务、节点提醒接口
7.  前端页面：推荐列表、赛事详情、组队大厅、协同任务面板
#### 上下游关联
复用RAG模块的LLM调用、向量嵌入能力，依赖用户认证模块的用户标签与身份信息。

---

### 模块4：智慧地图与AR导航模块（独立业务模块）
#### 核心开发目标
实现校园地图可视化、实物拍照识别、室内外导航，解决校园内地点查找、设施识别需求，**第四个开发**。
#### 流程闭环总结
先开发基础地图能力，再开发进阶的拍照识别与导航能力：
1.  **基础地图闭环**：校园POI数据采集 → MySQL POI库建设 → 2D/3D地图渲染 → 地点搜索/定位
2.  **拍照识别闭环**：用户调用相机拍摄 → AI图像识别 → 匹配POI库 → 地图标注+详情展示
3.  **导航闭环**：起点/终点确认 → 路线规划 → 2D/3D地图导航/AR实景导航 → 到达目的地
#### 必做开发节点
1.  校园POI库建设与地图基础渲染
2.  拍照识别模块：AI图像识别模型集成、POI匹配逻辑
3.  导航模块：路线规划、地图导航、AR实景导航适配
4.  前端地图页面与交互开发
#### 上下游关联
依赖用户认证模块，可复用RAG模块的地点详情检索能力。

---

### 模块5：校园论坛模块（独立业务模块）
#### 核心开发目标
实现校园内容发布、审核、互动的社区闭环，满足学生校内交流需求，**第五个开发**。
#### 流程闭环总结
分为两条并行闭环，先开发基础内容链路，再开发互动与审核能力：
1.  **内容生产审核闭环**：用户选择分区发帖 → 内容/附件上传 → AI/管理员内容审核 → 审核通过发布/驳回修改
2.  **内容浏览互动闭环**：用户进入分区 → 浏览热门/最新帖子 → 查看详情 → 点赞/评论/分享 → 热度排序更新
#### 必做开发节点
1.  论坛分区设计、MySQL核心表设计（帖子、评论、用户、分区表）
2.  基础接口开发：帖子发布、浏览、删除、评论、点赞接口
3.  内容审核模块：AI内容审核、管理员审核后台
4.  前端论坛页面与互动交互开发
#### 上下游关联
依赖用户认证模块的身份与权限控制，可复用RAG模块的内容检索能力。

---

## 三、开发里程碑与排期建议
| 里程碑 | 周期 | 核心交付内容 |
|--------|------|--------------|
| 里程碑1：基础底座搭建 | 1-2周 | 开发环境部署完成、用户认证模块全量上线、通用工具类封装完成、RAG模块离线数据链路跑通 |
| 里程碑2：AI核心能力开发 | 2-3周 | RAG检索问答模块全量上线、赛事模块「爬取→预处理→AI处理→入库」全链路跑通、数据可正常流入双库 |
| 里程碑3：核心业务MVP开发 | 3-4周 | 赛事模块学生端全流程上线、智慧地图核心功能上线、校园论坛基础功能上线 |
| 里程碑4：全系统联调优化 | 2周 | 全模块联调测试、模块间能力复用打通、前端体验优化、异常处理与性能优化 |
| 里程碑5：测试与迭代 | 1周 | 小范围用户测试、问题修复、功能迭代优化 |

---

## 四、开发关键避坑指南
1.  **复用优先**：将LLM调用、向量嵌入、数据库操作、文档处理封装成通用工具类，所有模块复用，杜绝重复开发。
2.  **MVP先行**：每个模块先跑通核心闭环，再做进阶功能。比如赛事模块先跑通「爬取→入库→推荐」，再做组队协同；地图模块先做2D地图，再做AR导航。
3.  **数据兜底**：所有爬取的原始数据、用户操作数据必须先兜底存入MySQL，再做处理，避免数据丢失。
4.  **权限隔离**：学生/教师/管理员的权限必须严格隔离，尤其是后台管理、内容审核、用户数据管理相关接口。
5.  **异常兜底**：爬虫失败、mineru转换失败、AI调用超时、数据库操作失败等场景，必须做异常捕获、重试机制和失败标记，避免系统崩溃。
