# 校园AI助手项目文件结构（分层分模块设计）
完全贴合你的技术栈（**Debian13 + Python/uv + FastAPI + MySQL/Milvus + Vue3**），采用**前后端分离架构**，后端严格遵循「**基础设施层→领域层→应用层→接口层**」的分层原则，前端按「**公共组件→业务页面→状态管理→API封装**」模块化组织，可直接作为项目初始化目录使用。

---

## 一、项目整体目录树
```
campus-ai-assistant/
├── backend/                 # Python后端（uv管理）
├── frontend/                # Vue3前端
├── docs/                    # 项目文档（需求、流程图、部署文档）
├── scripts/                 # 辅助脚本（数据库初始化、爬虫启动、mineru批量处理）
├── .gitignore
└── README.md
```

---

## 二、后端文件结构（Python/uv + FastAPI）
严格分层解耦，核心能力可复用，业务模块独立开发
```
backend/
├── pyproject.toml           # uv项目配置（依赖管理、项目元数据）
├── .env                      # 环境变量（数据库地址、API密钥、Milvus配置等）
├── alembic.ini               # Alembic数据库迁移配置
├── alembic/                  # 数据库迁移脚本目录
│   ├── versions/             # 迁移版本文件
│   └── env.py
├── src/
│   └── campus_ai/            # 后端核心包
│       ├── __init__.py
│       ├── main.py           # FastAPI应用入口
│       │
│       ├── core/             # 基础设施层（核心配置、通用工具、数据库连接）
│       │   ├── __init__.py
│       │   ├── config.py     # 全局配置加载（从.env读取）
│       │   ├── security.py   # JWT认证、密码哈希、权限校验
│       │   ├── database.py   # MySQL连接池、SQLAlchemy会话管理
│       │   ├── milvus.py     # Milvus连接、向量操作封装
│       │   └── utils/        # 通用工具类（可复用给所有业务模块）
│       │       ├── llm_client.py    # LLM调用封装（支持API/本地模型）
│       │       ├── embedder.py      # 向量嵌入生成封装
│       │       ├── mineru_helper.py # mineru工具集成（PDF转Markdown）
│       │       ├── crawler_base.py  # 爬虫基类（请求头伪装、代理池、延时）
│       │       └── text_cleaner.py  # 文本清洗、归一化工具
│       │
│       ├── models/           # 领域层（SQLAlchemy数据库模型）
│       │   ├── __init__.py
│       │   ├── user.py       # 用户相关表（用户、角色、权限、个人标签）
│       │   ├── rag.py        # RAG相关表（校园知识、分类、元数据）
│       │   ├── contest.py    # 赛事相关表（原始赛事、处理后赛事、组队、任务）
│       │   ├── map.py        # 地图相关表（POI、识别记录）
│       │   └── forum.py      # 论坛相关表（分区、帖子、评论、互动）
│       │
│       ├── schemas/          # 应用层（Pydantic API请求/响应模型）
│       │   ├── __init__.py
│       │   ├── user.py       # 用户认证、个人信息相关Schema
│       │   ├── rag.py        # RAG问答、知识管理相关Schema
│       │   ├── contest.py    # 赛事推荐、组队、任务相关Schema
│       │   ├── map.py        # 地图搜索、识别、导航相关Schema
│       │   └── forum.py      # 论坛发帖、互动相关Schema
│       │
│       ├── services/         # 应用层（业务逻辑核心，按模块独立开发）
│       │   ├── __init__.py
│       │   ├── user_service.py       # 用户认证、权限、个人信息管理
│       │   ├── rag_service.py        # RAG知识入库、检索问答
│       │   ├── contest/              # 赛事模块独立子目录（业务复杂）
│       │   │   ├── __init__.py
│       │   │   ├── crawler_service.py    # 赛事爬取、去重、原始数据存储
│       │   │   ├── preprocess_service.py # 文档预处理、mineru调用、文本清洗
│       │   │   ├── ai_process_service.py # AI信息提取、分档打标、合规校验
│       │   │   ├── storage_service.py    # 双库入库、数据关联、更新触发
│       │   │   ├── match_service.py      # 赛事推荐、队友匹配算法
│       │   │   └── team_service.py       # 组队申请、任务拆分、节点提醒
│       │   ├── map_service.py        # 地图POI管理、拍照识别、导航
│       │   └── forum_service.py      # 论坛发帖、审核、互动、热度排序
│       │
│       ├── api/              # 接口层（FastAPI路由，按模块独立组织）
│       │   ├── __init__.py
│       │   ├── deps.py       # 路由依赖（获取当前用户、权限校验依赖）
│       │   ├── v1/           # API v1版本路由
│       │   │   ├── __init__.py
│       │   │   ├── auth.py   # 认证相关路由（登录、注册、Token刷新）
│       │   │   ├── rag.py    # RAG问答相关路由
│       │   │   ├── contest.py# 赛事相关路由
│       │   │   ├── map.py    # 地图相关路由
│       │   │   └── forum.py  # 论坛相关路由
│       │   └── router.py     # 总路由聚合（v1路由挂载）
│       │
│       └── tasks/            # 定时任务（APScheduler）
│           ├── __init__.py
│           ├── contest_tasks.py  # 赛事定时爬取、增量更新任务
│           └── reminder_tasks.py # 赛事节点、作业截止提醒任务
│
└── tests/                    # 测试目录
    ├── __init__.py
    ├── test_auth.py
    ├── test_rag.py
    └── test_contest.py
```

---

## 三、前端文件结构（Vue3 + HTML/CSS）
按功能模块组织，公共组件与业务组件分离，状态管理与API封装独立
```
frontend/
├── package.json
├── vite.config.js            # Vite构建配置
├── .env.development          # 开发环境变量（后端API地址）
├── .env.production           # 生产环境变量
├── index.html
├── public/                   # 公共静态资源
│   ├── favicon.ico
│   └── images/               # 全局图片资源
│
└── src/
    ├── main.js               # Vue应用入口
    ├── App.vue               # 根组件
    ├── router/               # 路由配置
    │   └── index.js          # 总路由（按模块配置子路由）
    │
    ├── store/                # Pinia状态管理
    │   ├── index.js
    │   ├── modules/
    │   │   ├── user.js       # 用户状态（登录信息、个人标签、权限）
    │   │   ├── contest.js    # 赛事状态（推荐列表、组队信息、任务进度）
    │   │   └── map.js        # 地图状态（POI数据、导航状态）
    │
    ├── api/                  # API调用封装（统一管理后端接口）
    │   ├── request.js        # Axios实例配置（拦截器、Token管理）
    │   ├── auth.js           # 认证相关API
    │   ├── rag.js            # RAG问答相关API
    │   ├── contest.js        # 赛事相关API
    │   ├── map.js            # 地图相关API
    │   └── forum.js          # 论坛相关API
    │
    ├── utils/                # 前端工具函数
    │   ├── auth.js           # Token存储、用户信息存储
    │   ├── format.js         # 时间格式化、文本格式化
    │   └── validate.js       # 表单校验工具
    │
    ├── assets/               # 模块静态资源
    │   ├── styles/           # 全局样式、主题样式
    │   │   ├── global.css
    │   │   └── variables.css
    │   └── icons/            # 图标资源
    │
    ├── components/           # 公共组件（可复用给所有业务模块）
    │   ├── layout/           # 布局组件
    │   │   ├── Navbar.vue    # 顶部导航栏
    │   │   ├── Sidebar.vue   # 侧边栏
    │   │   └── Footer.vue    # 底部栏
    │   ├── common/           # 通用组件
    │   │   ├── Button.vue
    │   │   ├── Input.vue
    │   │   ├── Modal.vue
    │   │   └── Loading.vue
    │   └── map/              # 地图公共组件
    │       └── MapViewer.vue # 地图渲染组件
    │
    └── views/                # 业务页面（按模块独立组织）
        ├── auth/              # 认证模块页面
        │   ├── Login.vue
        │   └── Register.vue
        ├── home/              # 首页
        │   └── Home.vue
        ├── rag/               # RAG问答模块页面
        │   └── KnowledgeQa.vue
        ├── contest/           # 赛事模块页面（业务复杂，分子目录）
        │   ├── RecommendList.vue  # 赛事推荐列表页
        │   ├── ContestDetail.vue  # 赛事详情页
        │   ├── TeamHall.vue       # 组队大厅页
        │   └── TeamTask.vue       # 组队协同任务面板页
        ├── map/               # 地图模块页面
        │   ├── CampusMap.vue      # 校园地图主页面
        │   └── ArNavigation.vue   # AR导航页面
        └── forum/             # 论坛模块页面
            ├── ForumIndex.vue     # 论坛首页
            ├── PostDetail.vue     # 帖子详情页
            └── CreatePost.vue     # 发帖页
```

---

## 四、关键文件说明
### 后端核心文件
1.  `pyproject.toml`：uv项目配置，定义依赖（FastAPI、SQLAlchemy、pymilvus、mineru、APScheduler等）、脚本命令（启动服务、运行迁移、启动爬虫）
2.  `src/campus_ai/core/config.py`：全局配置单例，统一管理数据库地址、API密钥、Milvus配置、mineru路径等所有环境变量
3.  `src/campus_ai/core/utils/`：所有通用工具类封装在此，业务模块直接调用，避免重复开发
4.  `src/campus_ai/services/contest/`：赛事模块业务逻辑独立子目录，按流程拆分成多个服务文件，便于并行开发

### 前端核心文件
1.  `src/api/request.js`：Axios实例统一配置，实现请求拦截（自动加Token）、响应拦截（统一错误处理）
2.  `src/store/modules/`：按业务模块拆分Pinia状态，避免状态混乱
3.  `src/views/contest/`：赛事模块页面独立子目录，与后端业务模块一一对应，便于联调

