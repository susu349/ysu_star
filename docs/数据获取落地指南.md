# 数据获取落地操作指南

## 一、我们现在有什么

### 已完成的代码模块
```
backend/src/campus_ai/
├── models/
│   ├── knowledge.py    # 知识库数据模型（文档、分块、爬虫任务等）
│   └── user.py         # 用户模型
└── core/utils/
    ├── crawler_base.py      # 爬虫基础类
    └── document_processor.py # 文档处理（PDF/Markdown解析、分块）
```

### 现有数据
```
知识库/
├── ysu_学习资料/
│   └── 电子书/          # 已有PDF资源
├── 竞赛/
├── 社团/
└── ... (其他分类目录)
```

## 二、第一步：先导入现有数据（最简单）

### 操作流程

**1. 准备环境**（需要你帮忙）
```bash
cd /home/su/桌面/ysustar/backend
python -m venv .venv
source .venv/bin/activate
pip install -e .
```

**2. 启动数据库**（需要你帮忙）
```bash
# 使用 docker-compose 启动 MySQL 和 Milvus
cd /home/su/桌面/ysustar
docker-compose up -d mysql milvus
```

**3. 运行数据导入脚本**（我来写）

我会写一个脚本，自动扫描 `知识库/` 目录，把现有的PDF/Markdown文件导入系统。

## 三、第二步：手动录入第一批核心数据

### 优先级1：校园基础数据（最有用，数据量小）

| 数据项 | 建议格式 | 预计工作量 |
|--------|---------|-----------|
| 校园建筑列表 | CSV/Excel | 30分钟 |
| 院系专业介绍 | Markdown | 1小时 |
| 校史简介 | Markdown | 30分钟 |
| 常用电话/地点 | CSV | 15分钟 |

**怎么操作：**
1. 你先在 Excel 里整理好
2. 保存为 CSV 放到 `data/import/` 目录
3. 我写脚本导入数据库

## 四、第三步：爬虫获取官网数据

### 可爬取的目标

| 网站 | 内容 | 难度 |
|------|------|------|
| ysu.edu.cn | 学校简介、新闻 | 简单 |
| 各学院官网 | 院系介绍、师资 | 中等 |
| 教务处 | 课程信息（需要登录？） | 可能困难 |

**注意**：爬虫要慢，不要给学校网站造成压力。

## 五、需要你帮忙做的事

### 物理层面我做不了的：

1. **启动 Docker 服务**
   ```bash
   cd /home/su/桌面/ysustar
   docker-compose up -d
   ```

2. **如果需要浏览器操作**
   - 访问官网手动下载一些公开文档
   - 如有验证码的登录操作

3. **整理第一批数据**
   - 找一些现成的校园资料（PDF、Word）
   - 整理一个简单的建筑列表Excel

## 六、具体干活顺序

1. ✅ 设计数据模型（已完成）
2. ⏳ 你启动 Docker 数据库
3. ⏳ 我写数据库初始化脚本
4. ⏳ 我写数据导入脚本
5. ⏳ 你整理第一批数据文件
6. ⏳ 运行导入，验证数据

---

**现在需要你做的：**
1. 先看看 `docker-compose.yml` 能不能正常启动服务
2. 想想我们先用哪个小数据集做测试？
